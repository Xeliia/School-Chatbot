---
phase: 01-dynamic-faq-suggestions
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/faq_suggestions.py
  - backend/main.py
  - backend/tests/test_faq_suggestions.py
autonomous: true
requirements:
  - FAQ-02
  - FAQ-04
  - FAQ-05
  - FAQ-06
  - FAQ-07
must_haves:
  truths:
    - "API can return 3-4 randomized FAQ suggestions from a curated SCHA pool"
    - "Last-used suggestion is excluded from immediate next set"
    - "Suggestion payload contains stable IDs and display text"
  artifacts:
    - path: "backend/faq_suggestions.py"
      provides: "FAQ pool and deterministic filtering/random selection helper"
    - path: "backend/main.py"
      provides: "Authenticated GET /suggestions endpoint"
      exports: ["list_suggestions"]
    - path: "backend/tests/test_faq_suggestions.py"
      provides: "Automated checks for dedup and rotation rules"
  key_links:
    - from: "backend/main.py"
      to: "backend/faq_suggestions.py"
      via: "list endpoint calls selection helper"
      pattern: "select_suggestions"
    - from: "backend/main.py"
      to: "verify_api_key"
      via: "Depends auth gate"
      pattern: "Depends\(verify_api_key\)"
---

<objective>
Create backend support for quick-reply FAQ suggestions with server-side randomization and immediate-turn dedup filtering.

Purpose: Ensure frontend can request context-safe suggestion sets and avoid redundant immediate repeats.
Output: New suggestion selector module, authenticated API endpoint, and backend unit tests.
</objective>

<execution_context>
@$HOME/.copilot/get-shit-done/workflows/execute-plan.md
@$HOME/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-dynamic-faq-suggestions/01-CONTEXT.md
@backend/main.py
@backend/config.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create FAQ contracts and selection logic</name>
  <files>backend/faq_suggestions.py, backend/tests/test_faq_suggestions.py</files>
  <behavior>
    - Test 1: selector returns at most requested size and never returns duplicates in one response.
    - Test 2: selector excludes IDs provided in immediate exclusion list (per D-05).
    - Test 3: selector allows previously excluded IDs to reappear when exclusion list no longer includes them (per D-06).
  </behavior>
  <action>Implement a curated SCHA FAQ pool (15-20 entries) with stable IDs and categories per D-03. Add a pure helper like select_suggestions(pool, exclude_ids, limit, rng_seed=None) that filters exclude_ids, randomizes remaining items, and returns up to limit entries. Keep logic deterministic under test by supporting optional seed injection.</action>
  <verify>
    <automated>python -m unittest backend/tests/test_faq_suggestions.py -v</automated>
  </verify>
  <done>Selector module and tests exist, tests pass, and immediate-turn exclusion behavior is proven.</done>
</task>

<task type="auto">
  <name>Task 2: Expose authenticated suggestion endpoint</name>
  <files>backend/main.py</files>
  <action>Add GET /suggestions endpoint using existing Bearer auth (per D-02). Accept query params exclude_ids (comma-separated IDs) and limit (default 4, clamp 3-4 per D-02). Return JSON list of suggestion objects {id, question, category}. Reuse selector helper from backend/faq_suggestions.py. Keep endpoint independent from chat generation latency.</action>
  <verify>
    <automated>python -m py_compile backend/main.py backend/faq_suggestions.py</automated>
  </verify>
  <done>Endpoint returns authenticated randomized results, respects exclusion IDs, and enforces 3-4 item limit.</done>
</task>

<task type="auto">
  <name>Task 3: Add endpoint-level regression checks</name>
  <files>backend/tests/test_faq_suggestions.py</files>
  <action>Expand unittest coverage to include API-level behavior via FastAPI TestClient: unauthorized request returns 401, authorized request returns 200 with expected schema, and immediate exclusion IDs are absent from response.</action>
  <verify>
    <automated>python -m unittest backend/tests/test_faq_suggestions.py -v</automated>
  </verify>
  <done>Backend suggestion feature has repeatable automated regression checks for auth, schema, and dedup behavior.</done>
</task>

</tasks>

<verification>
- Run backend unit tests for selector and endpoint behavior.
- Start backend and validate GET /suggestions responds with 3-4 entries and no immediate excluded ID.
</verification>

<success_criteria>
- /suggestions exists and is protected by API key auth.
- Returned suggestions are randomized, category-aware, and immediate-turn dedup works.
- Automated backend tests pass locally.
</success_criteria>

<output>
After completion, create .planning/phases/01-dynamic-faq-suggestions/01-01-SUMMARY.md
</output>
