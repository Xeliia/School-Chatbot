---
phase: 01-dynamic-faq-suggestions
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - frontend/src/lib/suggestions.js
  - frontend/src/lib/suggestions.test.mjs
  - frontend/src/lib/components/SuggestedQuestions.svelte
  - frontend/src/App.svelte
autonomous: false
requirements:
  - FAQ-01
  - FAQ-03
  - FAQ-04
  - FAQ-05
  - FAQ-07
must_haves:
  truths:
    - "User sees 3-4 quick-reply bubbles above the input"
    - "Clicking a suggestion sends it as a user message and triggers chat request"
    - "Next suggestion set excludes the clicked button for one immediate turn"
  artifacts:
    - path: "frontend/src/lib/components/SuggestedQuestions.svelte"
      provides: "Bubble-button quick-reply UI"
    - path: "frontend/src/lib/suggestions.js"
      provides: "Suggestion turn-state helpers and exclusion tracking"
    - path: "frontend/src/App.svelte"
      provides: "API fetch, UI render, and click-to-send integration"
    - path: "frontend/src/lib/suggestions.test.mjs"
      provides: "Automated tests for dedup/rotation state transitions"
  key_links:
    - from: "frontend/src/App.svelte"
      to: "/suggestions"
      via: "fetch with Authorization header"
      pattern: "fetch\(.*\/suggestions"
    - from: "frontend/src/lib/components/SuggestedQuestions.svelte"
      to: "sendMessage flow"
      via: "onSelect callback sets input and triggers submit"
      pattern: "on:click"
---

<objective>
Integrate quick-reply FAQ suggestions into the Svelte chat UI, including turn-based exclusion and automatic send behavior.

Purpose: Improve discoverability and speed for common SCHA questions while preserving conversational flow.
Output: Suggestion component, frontend state helpers/tests, and App integration with backend endpoint.
</objective>

<execution_context>
@$HOME/.copilot/get-shit-done/workflows/execute-plan.md
@$HOME/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-dynamic-faq-suggestions/01-CONTEXT.md
@.planning/phases/01-dynamic-faq-suggestions/01-01-SUMMARY.md
@frontend/src/App.svelte
@frontend/src/lib/components/ChatInput.svelte

<interfaces>
From backend /suggestions response contract (Plan 01):
- Suggestion item: { id: string, question: string, category: string }
- Endpoint: GET /suggestions?exclude_ids=id1,id2&limit=4
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build suggestion turn-state helpers with tests</name>
  <files>frontend/src/lib/suggestions.js, frontend/src/lib/suggestions.test.mjs</files>
  <behavior>
    - Test 1: clicked suggestion ID is tracked as immediate exclusion for next fetch only (per D-05).
    - Test 2: exclusion state clears after next successful suggestion refresh, allowing later reappearance (per D-06).
    - Test 3: helper clamps display count to 3-4.
  </behavior>
  <action>Create pure helper utilities for suggestion state transitions (buildExcludeParam, onSuggestionClicked, onSuggestionsRefreshed). Keep logic framework-agnostic so tests run with node --test.</action>
  <verify>
    <automated>node --test frontend/src/lib/suggestions.test.mjs</automated>
  </verify>
  <done>Helpers encode dedup/rotation rules with passing automated tests.</done>
</task>

<task type="auto">
  <name>Task 2: Add quick-reply bubble component and App integration</name>
  <files>frontend/src/lib/components/SuggestedQuestions.svelte, frontend/src/App.svelte</files>
  <action>Create SuggestedQuestions component styled as rounded messenger-like bubbles (per D-01). In App.svelte, fetch suggestions from /suggestions using API key auth, render 3-4 buttons above ChatInput, and on click auto-send selected text by wiring into existing send flow (per D-04). Refresh suggestions after assistant response completes.</action>
  <verify>
    <automated>npm --prefix frontend run build</automated>
  </verify>
  <done>Buttons render and click-to-send works without breaking existing typed-message flow.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify quick-reply UX behavior in browser</name>
  <files>frontend/src/App.svelte, frontend/src/lib/components/SuggestedQuestions.svelte</files>
  <action>Run the integrated app and verify that visual placement and interaction behavior match the required quick-reply UX contract before phase sign-off.</action>
  <what-built>Frontend quick-reply bubbles integrated with backend suggestion endpoint and turn-based dedup logic.</what-built>
  <how-to-verify>
    1) Start backend and frontend.
    2) Confirm 3-4 suggestion bubbles appear above input on load.
    3) Click one suggestion and verify it is sent automatically.
    4) After assistant response, confirm next suggestion set does not include the just-clicked question.
    5) Send another turn and verify previously clicked question can reappear.
  </how-to-verify>
  <verify>
    <automated>npm --prefix frontend run build</automated>
  </verify>
  <resume-signal>Type "approved" or describe issues.</resume-signal>
  <done>User verifies visual style and behavioral rules match requirements.</done>
</task>

</tasks>

<verification>
- Run node helper tests and frontend production build.
- Perform one manual UX pass for dedup and reappearance behavior.
</verification>

<success_criteria>
- Quick-reply bubbles appear in the intended UI location and style.
- Clicking a bubble sends message automatically and advances suggestions.
- Immediate-turn dedup and next-turn reappearance rules are observable.
</success_criteria>

<output>
After completion, create .planning/phases/01-dynamic-faq-suggestions/01-02-SUMMARY.md
</output>
